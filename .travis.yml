language: android
android:
  components:
    - tools
    - platform-tools
    - tools

    # The SDK version used to compile your project
    - android-28

jdk:
  - oraclejdk8

cache:
  directories:
    - $HOME/.gradle/caches/
    - $HOME/.gradle/wrapper/
    - $HOME/.android/build-cache

  before_install:
    - java -v
    - which docker; echo

script:
  - make gradlewTravis
